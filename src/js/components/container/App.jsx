import React from "react";
import FormContainer from "./FormContainer";

// componentDidMount() {
//   request ('google.com/stats').then(res => console.log(res))
// };

export const App = () => <FormContainer />;
